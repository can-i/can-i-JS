import ConfigurationManager from '../index';


/**
 * These are all of the express settings.
 * All express settings should go here.
 * 
 */
let express_settings = ConfigurationManager.ExpressSettings;






let middleware_settings  = express_settings.MiddlewareSettings;

middleware_settings.Enable=true;

